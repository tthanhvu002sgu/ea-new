# RESULT REPORT — ID: 20260712-001

| Field | Value |
|-------|--------|
| Card / Prereg | `20260712-001` (`research/cards/20260712-001.md` / `research/prereg/20260712-001.md`) |
| Stage reached | G5 (Mechanism Test G5a) |
| Final decision | **KILL** |
| Trials used | `1` (Mechanism evaluation) / `5` budget |

---

## 0. Executive (FAIL-first)

- **Decision:** **KILL AT GATE 5a (MECHANISM TEST FAILURE)**
- **Primary kill reasons:**
  1. **Không có ý nghĩa thống kê về hướng (No Statistical Significance):** Trên 253 ngày có range H1 `07:00` mở rộng (`ratio > 1.25`) trong giai đoạn Discovery (`2020-01` đến `2023-06`), return cùng hướng trong 6 giờ tiếp theo (`08:00->14:00`) chỉ đạt trung bình `$0.5820` USD với `Win rate 49.80%` (`126/253` thắng, `127/253` thua). Kiểm định `t-test p-value = 0.1860` và `sign-test p-value = 0.5500` (đều $\ge 0.05$). Theo đúng Prereg Section 7: `p >= 0.05` $\rightarrow$ **KILL G5 ngay, không backtest full strategy**.
  2. **Gãy tính đơn điệu (Dose-Response Failure):** Khi chia mức ratio thành 3 bins, Bin 2 (`1.25 < ratio <= 1.50`, $N=89$) cho return trung bình **âm** (`-$0.2708` USD), thấp hơn cả Bin 1 (`$0.1747` USD). Không có tính chất "momentum ban đầu càng mạnh thì continuation càng tốt".
  3. **Gãy kiểm định thời điểm (Timing Shift Failure):** Dịch chuyển thời điểm vào lệnh trễ +1 bar (`09:00->15:00`) lại cho return trung bình **cao hơn** (`$0.8917` USD vs `$0.5820` USD lúc `08:00`). Điều này chứng minh tín hiệu `08:00` không bắt trúng dòng tiền concentrated order flow của phiên London, mà giá vàng chỉ trôi nổi theo xu hướng ngẫu nhiên trong ngày.

---

## 1. Mechanism test (G5a) — Discovery Data (`2020-01-01` to `2023-06-30`)

| Test | N | Stat / Measurement | Pass? | Note / Requirement |
|------|---|-------------------|-------|--------------------|
| **Event / conditional return** | 253 | Mean Ret $= \$0.5820$, WinRate $= 49.80\%$ ($126/253$).<br>`t-test stat = 0.8942`, **`p = 0.1860`**.<br>`sign-test` **`p = 0.5500`**. | **FAIL** | Prereg yêu cầu `p < 0.05` và `mean > 0`. Cả t-test và sign-test đều không có ý nghĩa thống kê. |
| **Dose-response (Monotonicity)** | 253 | Bin 1 ($1.00 - 1.25$, $N=173$): Mean $= \$0.1747$<br>Bin 2 ($1.25 - 1.50$, $N=89$): **Mean $= -\$0.2708$**<br>Bin 3 ($> 1.50$, $N=164$): Mean $= \$1.0448$ | **FAIL** | Prereg yêu cầu `Bin 3 > Bin 2 > Bin 1`. Thực tế `Bin 2 < Bin 1` (âm return). |
| **Timing (No Lookahead Shift +1 bar)** | 253 | Exact Entry (`08:00->14:00`): Mean $= \$0.5820$<br>Delayed Entry (`09:00->15:00`): **Mean $= \$0.8917$** | **FAIL** | Prereg yêu cầu delayed return phải giảm đi. Thực tế trễ 1 bar lại lãi cao hơn $+53\%$. |

---

## 2. Variants attempted (full disclosure)

| # | Change vs base | Primary metric (`expectancy_R_after_cost`) | Kill? | Why |
|---|----------------|-------------------------------------------|-------|-----|
| 0 | G5a Mechanism Test Only (Raw continuation return without SL/TP execution) | $N=253$, Mean $= \$0.5820$ USD ($< 0.6 \text{ pts}$ gross). Với slippage + spread = `35 pts` ($3.50/oz) theo cost model $\rightarrow$ Net Expectancy **chắc chắn âm thảm họa**. | **YES (KILL)** | Fails statistical significance ($p = 0.186$), fails monotonicity, fails timing shift test. |
| 1 | Baseline Moderate (`threshold = 1.25, holding = 6h, SL = 1.5 ATR`) | `SKIPPED (NOT RUN)` | **KILL** | Dừng theo quy tắc G5: Mechanism FAIL $\rightarrow$ Không build full strategy để tránh curve-fit overfit. |
| 2 | High Momentum (`threshold = 1.50, holding = 6h, SL = 1.5 ATR`) | `SKIPPED (NOT RUN)` | **KILL** | Dừng theo quy tắc G5. |
| 3 | Fast Exit (`threshold = 1.25, holding = 4h, SL = 1.5 ATR`) | `SKIPPED (NOT RUN)` | **KILL** | Dừng theo quy tắc G5. |
| 4 | Extended Holding (`threshold = 1.25, holding = 8h, SL = 1.5 ATR`) | `SKIPPED (NOT RUN)` | **KILL** | Dừng theo quy tắc G5. |
| 5 | Tighter Stop (`threshold = 1.25, holding = 6h, SL = 1.0 ATR`) | `SKIPPED (NOT RUN)` | **KILL** | Dừng theo quy tắc G5. |

---

## 3. In-sample (limited) — after cost

*Vì cơ chế lõi (Mechanism Test G5a) đã **FAIL** toàn diện, theo mục 2 của yêu cầu G5 và Hiến pháp AI, toàn bộ việc chạy In-Sample full strategy (G5b) với SL/TP và Lot size được **HỦY BỎ** để không lãng phí tài nguyên và không tạo ảo tưởng về edge từ việc stack SL/TP.*

---

## 4. Robustness (G6)

`SKIPPED (NOT APPLICABLE DUE TO G5a KILL)`

---

## 5. OOS / Holdout (G7) — only if opened under protocol

* **Validation Split (`2023-07` to `2024-12`):** `NOT TOUCHED`
* **Holdout Final Split (`2025-01` to `2026-06`):** **100% SEALED & UNTOUCHED**. Không có bất kỳ rò rỉ hay can thiệp nào vào tập dữ liệu mù.

---

## 6. Economic review (G9)

| Question | Answer |
|----------|--------|
| Who pays? | Đã bác bỏ: Không có bằng chứng thống kê cho thấy Asian faders hoặc mandatory hedgers tại London open bị bóc lột đủ mạnh để tạo ra một đà tiếp diễn có thể giao dịch sau chi phí trên XAUUSD. |
| If 50 funds copy, edge left? | N/A — Cơ chế gốc đã không tồn tại ngay trên tập dữ liệu lịch sử thô (gross before cost). |
| Intermediate confirmation exists? | Không. Shift test cho thấy giá di chuyển sau 09:00 độc lập với nến 07:00. |
| Live kill monitor defined? | N/A — Chiến lược bị loại ngay tại G5. |

---

## 7. Promotion

- [ ] Eligible for MQL5 EA coding
- [ ] Eligible for shadow live
- [x] **NOT eligible — archive / self-kill**

---

### Cập nhật Nhật ký Quyết định (`decision_log.md`)
Quyết định `KILL` chính thức này sẽ được log vào `research/decision_log.md` để ghi nhớ mẫu thất bại (pattern of failure) và chuyển sang ý tưởng/card mới.

**Signed human:** Vu Date: 20260712
