# PREREGISTRATION — ID: 20260712-001

| Field | Value |
|-------|--------|
| Card ID | `20260712-001` |
| Status | DRAFT |
| Locked date | |
| Locked by | (human only) |

> Khóa file này **TRƯỚC** khi chạy backtest / optimizer.  
> Sau khi xem kết quả: **cấm** sửa kill floors để cứu strategy.

---

## 1. Hypothesis under test

Nếu XAUUSD tạo một directional move lớn hơn median range trong 60 phút đầu của London session (07:00–08:00 UTC+2), thì action **tiếp tục theo hướng move đó** (entry tại close bar H1 07:00) có kỳ vọng return dương trong horizon **3–6 giờ tiếp theo** (trước khi NY session đóng), sau chi phí, vì **dòng order flow tập trung từ institutional rebalancing + hedging tại London open tạo momentum ngắn hạn mà retail và Asian-session participants chưa kịp phản ánh**.

## 2. Data protocol

| Split | Start | End | Allowed use |
|-------|-------|-----|-------------|
| Discovery | `2020-01-01` | `2023-06-30` | mechanism + limited IS |
| Validation | `2023-07-01` | `2024-12-31` | robustness / WF |
| Holdout final | `2025-01-01` | `2026-06-12` | **sealed** until G7 sign-off |

- Bar type / TF: `M15, H1` (tổng hợp từ source file `XAUUSD_M1_202001020600_202606122259.csv`)
- Session filter (fixed): London Open (`07:00–11:00 UTC+2`) để đánh giá signal bar và entry; NY Overlap (`13:00–17:00 UTC+2`) là giới hạn time-based exit.
- Cost model ref: `constraints.yaml` → `cost_model` (`pessimistic`: spread `25` points, slippage `10` points, commission `$7.0`/lot roundturn, swap `enabled`).

## 3. Primary metric (đúng 1)

- Primary: `expectancy_R_after_cost` (Expectancy tính theo R-multiple trên mỗi lệnh, đã trừ toàn bộ spread, slippage, commission và swap).
- Direction for PASS: `expectancy_R_after_cost >= 0.05` R per trade (với CI 95% cận dưới > 0 hoặc bootstrap p-value < 0.05).

## 4. Secondary metrics (chẩn đoán only — không dùng để cứu)

- `profit_factor` (kỳ vọng > 1.25 sau cost)
- `max_drawdown_pct` (kỳ vọng <= 15.0%)
- `win_rate` (chỉ để theo dõi phân phối, không ràng buộc floor vì phụ thuộc R:R)
- `avg_trade` (R và points thuần sau chi phí)
- `top5pct_trade_contribution` (kỳ vọng <= 35% tổng lợi nhuận)

## 5. Hard kill floors (copy/adjust from constraints, then LOCK)

| Floor | Value |
|-------|--------|
| min trades (discovery) | `150` trades |
| min trades (validation) | `80` trades |
| min primary metric after cost | `0.05` R |
| max DD % | `15.0%` |
| max top 5% trade profit share | `35.0%` (`0.35`) |
| min positive subperiods (of 3) | `2` subperiods |
| permutation: edge must die | `YES` (`require_permutation_edge_die: true`) |
| cost stress | `1.5×` (expectancy > 0) and `2.0×` (không sụp đổ tài khoản / giải thích được capacity) |

## 6. Trial budget

| Item | Limit |
|------|--------|
| Max variants this family | `5` variants |
| Max free params | `3` params (`threshold`, `holding_hours`, `sl_atr_mult`) |
| Allowed param names | `threshold`, `holding_hours`, `sl_atr_mult` |
| Forbidden post-hoc filters | all not listed above (Cấm thêm RSI, MA, Bollinger, MACD, time-of-day subfilter, ML filters) |

Variants planned (liệt kê trước, không thêm sau):

1. **Variant 1 (Baseline moderate):** `threshold = 1.25`, `holding_hours = 6`, `sl_atr_mult = 1.5`
2. **Variant 2 (High momentum threshold):** `threshold = 1.50`, `holding_hours = 6`, `sl_atr_mult = 1.5`
3. **Variant 3 (Fast exit / Short holding):** `threshold = 1.25`, `holding_hours = 4`, `sl_atr_mult = 1.5`
4. **Variant 4 (Extended holding to NY overlap):** `threshold = 1.25`, `holding_hours = 8`, `sl_atr_mult = 1.5`
5. **Variant 5 (Tighter stop):** `threshold = 1.25`, `holding_hours = 6`, `sl_atr_mult = 1.0`

## 7. Mechanism test (chạy TRƯỚC full strategy)

| Test | Spec | Pass condition |
|------|------|----------------|
| **Event / conditional return** | Khi H1 bar `07:00` có `range > median_range(20) * 1.25`, đo mean return của khoảng `08:00 -> 14:00` cùng hướng với `return(07:00)`. | Mean conditional return cùng hướng > 0 có ý nghĩa thống kê (`sign-test p < 0.05` hoặc `t-test p < 0.05`) so với random unconditional return. Nếu `< 0` hoặc `p >= 0.05` $\rightarrow$ **KILL G5 ngay, không backtest**. |
| **Dose-response (nếu có)** | Chia các bar `07:00` theo độ lớn của `range / median_range(20)` thành 3 bins: Bin 1 (`1.0 - 1.25`), Bin 2 (`1.25 - 1.50`), Bin 3 (`> 1.50`). Đo mean continuation return mỗi bin. | Tính đơn điệu (Monotonicity): `Bin 3 > Bin 2 > Bin 1`. Momentum bar đầu càng lớn thì continuation phía sau càng mạnh. |
| **Timing (no lookahead)** | Kiểm tra shift test: Dịch chuyển thời điểm lấy tín hiệu chậm +1 bar (`09:00` thay vì `08:00`). | Expectancy tại `+1 bar delay` phải giảm đi hoặc giữ ở mức thấp hơn entry chuẩn (`08:00`), chứng minh tín hiệu bắt trúng dòng tiền lúc London open, không bị leakage từ dữ liệu tương lai. |

## 8. Robustness battery (G6) — checklist

- [ ] Param sensitivity ±20% (hoặc neighbor grid thô)
- [ ] Time subperiods (3)
- [ ] Regime split (vol high/low hoặc trend/range proxy)
- [ ] Drop top 5% winners
- [ ] Entry delay +1 bar
- [ ] Cost ×1.5 / ×2
- [ ] Permutation / label shuffle

## 9. OOS rule

- [ ] One-shot validation — no tune
- [ ] Holdout final only after G6 PASS + human sign
- [ ] Fail OOS → KILL family (new card required)

## Human lock

**I lock this protocol before seeing results.**  
Signed: Vu Date: 20260712
